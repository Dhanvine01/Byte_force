# Privacy Gatekeeper — Desktop App Requirements

# ── GUI Framework ──────────────────────────────────────────────────────────
PyQt6>=6.6.0

# ── Team 1 (Intelligence Engine) ──────────────────────────────────────────
fastapi>=0.110.0          # kept for schemas.py compatibility
pydantic>=2.6.0
pytesseract>=0.3.10
pdfplumber>=0.10.3
Pillow>=10.2.0
opencv-python-headless>=4.9.0.80
numpy>=1.26.0

# ── PDF Export (optional but recommended) ─────────────────────────────────
PyMuPDF>=1.24.0           # pip install PyMuPDF — for PDF redaction export

# ── Packaging to .exe ─────────────────────────────────────────────────────
# pyinstaller>=6.5.0      # uncomment when ready to build .exe
